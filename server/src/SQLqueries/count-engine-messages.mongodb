
use('squadhelp-chat-dev');

// db.getName();
// db.getCollectionNames();

db.messages.find({body: /паровоз/gi}).count();

db.messages.aggregate([
  {$match: {body: /паровоз/gi}},
  {$count: 'count_engine_messages'}
]);

db.messages.count({body: /паровоз/gi});
